<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ozadje -->
    <solid android:color="@android:color/white" />

    <!-- Obroba -->
    <stroke
        android:width="2dp"
        android:color="@color/borderRecyclerItem" />

    <!-- Zaobljeni robovi -->
    <corners android:radius="8dp" />
</shape>
